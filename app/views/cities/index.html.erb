<p id="notice"><%= notice %></p>

<h1>Cities</h1>

<table>
  <thead>
    <tr>
      <th>City name</th>
      <th>State</th>
      <th>Country</th>
      <th>Max latitude</th>
      <th>Min latitude</th>
      <th>Max longitude</th>
      <th>Min longitude</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cities.each do |city| %>
      <tr>
        <td><%= city.city_name %></td>
        <td><%= city.state_id %></td>
        <td><%= city.country_id %></td>
        <td><%= city.max_latitude %></td>
        <td><%= city.min_latitude %></td>
        <td><%= city.max_longitude %></td>
        <td><%= city.min_longitude %></td>
        <td><%= link_to 'Show', city %></td>
        <td><%= link_to 'Edit', edit_city_path(city) %></td>
        <td><%= link_to 'Destroy', city, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New City', new_city_path %>
